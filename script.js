function login()
{
  let u = username.value
  let p = pw.value
  if (u=="truc" && p=="bi")
  {
    window.open("home.html")
    document getElementById(compiler).style.display="block"
  }
}
